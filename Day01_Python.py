# Prints Welcome Message
print("Welcome to Band Name Generator")

# Creates string variable based on user input on the city they grew up in.
cityname = input("What's the name of the city you grew up in? ")

# Creates string variable based on user input on their pet's name.
petname = input("What's your pet's name? ")

# Prints both variables with a space in between, generating the user's band name.
print(cityname + ' ' + petname)

